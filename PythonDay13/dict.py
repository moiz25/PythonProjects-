# Generating a dictionary where keys are numbers between 1 and 20 (both included) and values are squares of keys
square_dict = {x: x**2 for x in range(1, 21)}

# Printing the generated dictionary
print("Dictionary with keys as numbers between 1 and 20 and values as squares of keys:")
print(square_dict)
